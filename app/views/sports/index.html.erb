<nav>
  <div class="ui secondary pointing menu">
      <%= link_to @user.name, user_path(@user), class:"item" %>
      <%= link_to "Events", events_path, class:"item" %>
      <%= link_to "Sports", sports_path, class:"item active" %>
    <div class="right menu">
      <!-- <a class="ui item"> -->
      <%= link_to "Logout", logout_path, method: :delete, class:"ui item" %>
      <!-- </a> -->
    </div>
  </div>
</nav>
<div class="basketwater_image">
  <div class="ui raised very padded text container segment" id="splash-box">
    <h1>Sports</h1>
    <button class="ui blue basic button">
      <%= link_to "Create a New Sport", new_sport_path %>
    </button>
    <br><br>
    <% @sports.each do |s| %>
      <%= link_to s.name, sport_path(s) %><br><br>
    <% end %>
  </div>
</div>
